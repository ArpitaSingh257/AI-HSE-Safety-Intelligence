# Server
PORT=5000
CORS_ORIGIN=http://localhost:5173

# Database (use your own MongoDB Atlas connection string)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/sih26165?retryWrites=true&w=majority

# Security
JWT_SECRET=replace_with_a_long_random_string

# AI Service (optional - falls back to a stub response if unreachable)
AI_SERVICE_URL=http://localhost:4000/api/v1/analyze

# Runtime mode
NODE_ENV=development